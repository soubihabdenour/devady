Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Front-controller routing: anything not a real file/directory goes to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>
